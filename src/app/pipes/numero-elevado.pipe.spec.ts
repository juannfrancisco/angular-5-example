import { NumeroElevadoPipe } from './numero-elevado.pipe';

describe('NumeroElevadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroElevadoPipe();
    expect(pipe).toBeTruthy();
  });
});
